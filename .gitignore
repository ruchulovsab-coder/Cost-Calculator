# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/
ENV/

# Streamlit
.streamlit/secrets.toml

# Testing
.pytest_cache/
.coverage
htmlcov/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Generated reports / scratch
*.tmp
~$*.xlsx

# Real rate cards live in Azure Blob Storage (loaded at runtime), never in the repo.
# Keep the local copy out of git so real rate data can't be committed by accident.
# (sample_rate_card.xlsx is the tracked, non-sensitive sample fixture.)
genus_rate_card.xlsx
